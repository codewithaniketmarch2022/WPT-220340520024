const express = require("express");

const mysql = require("mysql");

// Create connection

const db = mysql.createConnection({
  host: "localhost",
  user: "root",
  password: "cdac",
  database: "bookinfodb",
  });
  
  // Connect to MySQL

db.connect((err) => {
  if (err) {
    throw err;
  }
  console.log("MySql Connected");
});

const app = express();

// Update Book

app.get("/updatebook/:id", (req, res) => {
  let newName = "Updated name";
  let sql = `UPDATE book SET name = '${newName}' WHERE id = ${req.params.id}`;
  let query = db.query(sql, (err) => {
    if (err) {
      throw err;
    }

    res.send("Book updated...");

  });

});




               







